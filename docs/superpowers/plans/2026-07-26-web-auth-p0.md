# Web 认证 P0 实现计划（身份与鉴权地基）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 supernova Web 控制台加上多用户身份认证（预置账号/登录登出/server session），所有 `/api/*` 受保护，前端加 claude 风分屏登录页 + 路由守卫。

**Architecture:** 后端 FastAPI 新增 `auth/` 模块（SQLite 存 users+sessions、bcrypt 密码、cookie session、CSRF、auth middleware、login/logout/me 路由）；前端加 `AuthContext` + `RequireAuth` 守卫，fetch client 注入 `credentials` + CSRF header + 401 处理，新增 claude 风分屏 `LoginPage`。凭证机制 = server session + HttpOnly cookie + SQLite（spec §4.1 方案 A）。

**Tech Stack:** Python（FastAPI、sqlite3 stdlib、passlib[bcrypt]、pydantic）；React 18 + react-router-dom 6.26 + Tailwind 3 + shadcn/ui(Radix) + vitest + @testing-library/react + jsdom + MSW。

## Global Constraints

- **范围严格 P0**：只做身份认证。**不碰** workspace 成员制/产物隔离（P1）、repos 隔离（P2）、配置隔离（P3c）。现有 `/api/*` 路由只加"要求登录"保护，不加任何 per-user/per-workspace 过滤。
- **后端 env 前缀 `SUPERNOVA_`**；`WebConfig` 是 `@lru_cache get_config()` 单例，测试靠 `packages/web/tests/conftest.py` 的 `_reset_config` autouse fixture 清缓存（已存在，勿重造）。
- **新增依赖**：仅 `passlib[bcrypt]`（加到 `packages/web/pyproject.toml` `dependencies`）。`sqlite3` 是 stdlib 不加。前端**不加**新依赖（UserMenu 用已有 `@radix-ui/react-popover`）。
- **测试陷阱（memory）**：后端只跑改动测试文件，**勿跑全套**（Temporal/网络慢测试 hang）。前端命令**必须** `cd packages/web/frontend`（cwd 不持久）；Radix 组件测试优先受控写法。
- **i18n 陷阱（memory）**：`locales/zh.json` 的值必须是**真中文**（不能漏翻成英文）；`locales/en.json` 同步加英文。fallbackLng = `zh`。
- **DSF（memory）**：双主题 `:root`(暗)/`.light`(亮)；语义色 `--c-orange`；字体 `--font-serif`(IBM Plex Serif)；`--primary` = coral(15°, #D97757)；`--radius: 12px`。登录页**强制亮主题**（根容器加 `.light` class），不跟随系统/用户主题。
- **TDD**：每个 task 先写失败测试 → 验证失败 → 最小实现 → 验证通过 → commit。
- **SQLite 访问**：同步 `sqlite3`，每个操作用短连接 `with sqlite3.connect(db_path) as conn`（sqlite 自带文件锁，本地工具低并发足够，无需 aiosqlite）。
- **cookie 名**：会话 `sn-sid`（HttpOnly），CSRF `sn-csrf`（非 HttpOnly，前端可读）。session id = `secrets.token_urlsafe(32)`。
- **CSRF 模式**：double-submit——登录时 set `sn-csrf` cookie；写操作（POST/PUT/DELETE）必须带 `X-CSRF-Token` header == cookie 值。

## File Structure

**后端 Create：**
- `packages/web/src/supernova_web/auth/__init__.py`（空）
- `packages/web/src/supernova_web/auth/passwords.py` — bcrypt 哈希/校验
- `packages/web/src/supernova_web/auth/models.py` — `User` / `SessionRow` pydantic
- `packages/web/src/supernova_web/auth/store.py` — SQLite 建表 + users/sessions CRUD
- `packages/web/src/supernova_web/auth/session.py` — session 创建/校验/撤销/清理
- `packages/web/src/supernova_web/auth/seed.py` — `users.yaml` → SQLite upsert（不覆盖已存在）
- `packages/web/src/supernova_web/auth/brute.py` — 登录失败计数 + 临时锁定
- `packages/web/src/supernova_web/auth/csrf.py` — double-submit 校验
- `packages/web/src/supernova_web/auth/middleware.py` — auth middleware（cookie→session→user）
- `packages/web/src/supernova_web/auth/dependencies.py` — `current_user` / `require_admin`
- `packages/web/src/supernova_web/auth/routes.py` — `/api/auth/{csrf,login,logout,me}` router
- `scripts/web_hash_password.py` — 交互生成 bcrypt hash
- `configs/users.yaml.example` — 预置账号示例

**后端 Modify：**
- `packages/web/src/supernova_web/config.py` — WebConfig 加 auth 字段
- `packages/web/src/supernova_web/app.py` — 注册 auth router + auth middleware + startup seed/purge；现有 `/api/*` 路由加 `Depends(current_user)`
- `packages/web/src/supernova_web/api/{workspaces,scan,multi_configs,repos,events,fs,system_status}.py` — 路由加 `Depends(current_user)`
- `packages/web/pyproject.toml` — 加 `passlib[bcrypt]`
- `.env.example` — 文档化 auth env

**后端 Test（create）：**
- `packages/web/tests/test_auth_passwords.py`
- `packages/web/tests/test_auth_store.py`（含 models）
- `packages/web/tests/test_auth_session.py`
- `packages/web/tests/test_auth_seed.py`
- `packages/web/tests/test_auth_brute.py`
- `packages/web/tests/test_auth_csrf.py`
- `packages/web/tests/test_auth_middleware.py`
- `packages/web/tests/test_auth_routes.py`

**前端 Create：**
- `packages/web/frontend/src/auth/AuthContext.tsx` — Provider + `useAuth`
- `packages/web/frontend/src/auth/RequireAuth.tsx` — 路由守卫
- `packages/web/frontend/src/components/layout/UserMenu.tsx` — 用户菜单（Popover）
- `packages/web/frontend/src/pages/LoginPage.tsx` — claude 风分屏登录页

**前端 Modify：**
- `packages/web/frontend/src/api/client.ts` — credentials + CSRF header + 401 处理 + options
- `packages/web/frontend/src/App.tsx` — 包 `AuthProvider`
- `packages/web/frontend/src/router.tsx` — `/login` 公开路由 + 业务路由包 `RequireAuth`
- `packages/web/frontend/src/components/layout/TopBar.tsx` — 加 `UserMenu`
- `packages/web/frontend/src/locales/{zh,en}.json` — 加 `auth.*`

**前端 Test（create）：**
- `packages/web/frontend/src/api/client.test.ts`
- `packages/web/frontend/src/auth/AuthContext.test.tsx`
- `packages/web/frontend/src/auth/RequireAuth.test.tsx`
- `packages/web/frontend/src/pages/LoginPage.test.tsx`

---

## Task 1: 密码哈希（passwords.py + passlib 依赖）

**Files:**
- Modify: `packages/web/pyproject.toml`（dependencies 加 `passlib[bcrypt]`）
- Create: `packages/web/src/supernova_web/auth/__init__.py`（空文件）
- Create: `packages/web/src/supernova_web/auth/passwords.py`
- Test: `packages/web/tests/test_auth_passwords.py`

**Interfaces:**
- Produces: `hash_password(plain: str) -> str`、`verify_password(plain: str, hashed: str) -> bool`

- [ ] **Step 1: 加依赖**

编辑 `packages/web/pyproject.toml`，在 `dependencies = [...]` 列表里加一行 `"passlib[bcrypt]",`（紧跟 `"uvicorn[standard]>=0.29",` 之后）。然后跑 `uv sync --all-packages`（memory：必须 `--all-packages` 否则静默卸载 worker）。

- [ ] **Step 2: 写失败测试**

```python
# packages/web/tests/test_auth_passwords.py
from supernova_web.auth.passwords import hash_password, verify_password


def test_hash_and_verify_roundtrip():
    h = hash_password("s3cret-pass")
    assert h != "s3cret-pass"
    assert h.startswith("$2")  # bcrypt
    assert verify_password("s3cret-pass", h) is True


def test_wrong_password_rejected():
    h = hash_password("s3cret-pass")
    assert verify_password("wrong", h) is False


def test_each_hash_unique_salt():
    assert hash_password("same") != hash_password("same")
```

- [ ] **Step 3: 验证失败**

Run: `cd /Users/mango/project/shannon-refactor/shannon-py && uv run pytest packages/web/tests/test_auth_passwords.py -v`
Expected: FAIL（`ModuleNotFoundError: supernova_web.auth.passwords`）

- [ ] **Step 4: 实现**

```python
# packages/web/src/supernova_web/auth/passwords.py
from __future__ import annotations

from passlib.context import CryptContext

_pwd = CryptContext(schemes=["bcrypt"], deprecated="auto")


def hash_password(plain: str) -> str:
    return _pwd.hash(plain)


def verify_password(plain: str, hashed: str) -> bool:
    return _pwd.verify(plain, hashed)
```

`packages/web/src/supernova_web/auth/__init__.py` 留空。

- [ ] **Step 5: 验证通过**

Run: `cd /Users/mango/project/shannon-refactor/shannon-py && uv run pytest packages/web/tests/test_auth_passwords.py -v`
Expected: 3 passed

- [ ] **Step 6: Commit**

```bash
cd /Users/mango/project/shannon-refactor/shannon-py
git add packages/web/pyproject.toml uv.lock packages/web/src/supernova_web/auth/__init__.py packages/web/src/supernova_web/auth/passwords.py packages/web/tests/test_auth_passwords.py
git commit -m "feat(web/auth): bcrypt 密码哈希 (passlib) + 测试"
```

---

## Task 2: SQLite 存储层 + 数据模型（store.py + models.py）

**Files:**
- Create: `packages/web/src/supernova_web/auth/models.py`
- Create: `packages/web/src/supernova_web/auth/store.py`
- Test: `packages/web/tests/test_auth_store.py`

**Interfaces:**
- Consumes: 无
- Produces:
  - `models.User(id:int, username:str, role:str)`、`models.SessionRow(id:str, user_id:int, expires_at:str, last_seen_at:str)`（pydantic BaseModel）
  - `store.AuthStore(db_path: str)`，方法：`init_schema()`、`create_user(username, password_hash, role="user") -> User`、`get_user_by_username(username) -> User | None`、`get_user(user_id) -> User | None`、`insert_session(row: SessionRow) -> None`、`get_session(sid) -> SessionRow | None`、`update_session_seen(sid, iso_ts) -> None`、`delete_session(sid) -> None`、`purge_expired(now_iso: str) -> int`

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_store.py
from supernova_web.auth.models import User, SessionRow
from supernova_web.auth.store import AuthStore


def test_init_schema_idempotent(tmp_path):
    db = tmp_path / "auth.db"
    s = AuthStore(str(db))
    s.init_schema()
    s.init_schema()  # 再次建表不报错
    assert db.exists()


def test_create_and_get_user(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    u = s.create_user("alice", "$2b$12$hash", role="user")
    assert u.id is not None and u.username == "alice" and u.role == "user"
    got = s.get_user_by_username("alice")
    assert got is not None and got.id == u.id
    assert s.get_user_by_username("nobody") is None
    assert s.get_user(u.id).username == "alice"


def test_username_unique(tmp_path):
    import pytest
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    s.create_user("alice", "h")
    with pytest.raises(Exception):
        s.create_user("alice", "h")


def test_session_crud_and_purge(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    u = s.create_user("alice", "h")
    s.insert_session(SessionRow(id="sid-1", user_id=u.id, expires_at="2099-01-01T00:00:00", last_seen_at="2026-01-01T00:00:00"))
    s.insert_session(SessionRow(id="sid-old", user_id=u.id, expires_at="2020-01-01T00:00:00", last_seen_at="2019-01-01T00:00:00"))
    assert s.get_session("sid-1").user_id == u.id
    assert s.get_session("missing") is None
    s.update_session_seen("sid-1", "2026-06-01T00:00:00")
    n = s.purge_expired("2026-01-01T00:00:00")
    assert n == 1  # sid-old 过期被删
    assert s.get_session("sid-old") is None
    s.delete_session("sid-1")
    assert s.get_session("sid-1") is None
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_store.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 models.py**

```python
# packages/web/src/supernova_web/auth/models.py
from __future__ import annotations

from pydantic import BaseModel


class User(BaseModel):
    id: int
    username: str
    role: str = "user"  # 'admin' | 'user'


class SessionRow(BaseModel):
    id: str
    user_id: int
    expires_at: str  # ISO8601
    last_seen_at: str  # ISO8601
```

- [ ] **Step 4: 实现 store.py**

```python
# packages/web/src/supernova_web/auth/store.py
from __future__ import annotations

import sqlite3
from typing import Iterable

from .models import SessionRow, User

_SCHEMA = """
CREATE TABLE IF NOT EXISTS users (
  id INTEGER PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  role TEXT NOT NULL DEFAULT 'user',
  created_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS sessions (
  id TEXT PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  created_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  last_seen_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS schema_meta (key TEXT PRIMARY KEY, value TEXT);
"""


class AuthStore:
    def __init__(self, db_path: str) -> None:
        self.db_path = db_path

    def _conn(self) -> sqlite3.Connection:
        return sqlite3.connect(self.db_path)

    def init_schema(self) -> None:
        with self._conn() as c:
            c.executescript(_SCHEMA)

    def create_user(self, username: str, password_hash: str, role: str = "user") -> User:
        from datetime import datetime, timezone
        now = datetime.now(timezone.utc).isoformat()
        with self._conn() as c:
            cur = c.execute(
                "INSERT INTO users(username, password_hash, role, created_at) VALUES(?,?,?,?)",
                (username, password_hash, role, now),
            )
            return User(id=cur.lastrowid, username=username, role=role)

    def get_user_by_username(self, username: str) -> User | None:
        with self._conn() as c:
            row = c.execute(
                "SELECT id, username, role FROM users WHERE username=?", (username,)
            ).fetchone()
        return User(id=row[0], username=row[1], role=row[2]) if row else None

    def get_user(self, user_id: int) -> User | None:
        with self._conn() as c:
            row = c.execute(
                "SELECT id, username, role FROM users WHERE id=?", (user_id,)
            ).fetchone()
        return User(id=row[0], username=row[1], role=row[2]) if row else None

    def insert_session(self, row: SessionRow) -> None:
        from datetime import datetime, timezone
        created = datetime.now(timezone.utc).isoformat()
        with self._conn() as c:
            c.execute(
                "INSERT INTO sessions(id, user_id, created_at, expires_at, last_seen_at) VALUES(?,?,?,?,?)",
                (row.id, row.user_id, created, row.expires_at, row.last_seen_at),
            )

    def get_session(self, sid: str) -> SessionRow | None:
        with self._conn() as c:
            row = c.execute(
                "SELECT id, user_id, expires_at, last_seen_at FROM sessions WHERE id=?", (sid,)
            ).fetchone()
        return SessionRow(id=row[0], user_id=row[1], expires_at=row[2], last_seen_at=row[3]) if row else None

    def update_session_seen(self, sid: str, iso_ts: str) -> None:
        with self._conn() as c:
            c.execute("UPDATE sessions SET last_seen_at=? WHERE id=?", (iso_ts, sid))

    def delete_session(self, sid: str) -> None:
        with self._conn() as c:
            c.execute("DELETE FROM sessions WHERE id=?", (sid,))

    def purge_expired(self, now_iso: str) -> int:
        with self._conn() as c:
            cur = c.execute("DELETE FROM sessions WHERE expires_at < ?", (now_iso,))
            return cur.rowcount
```

- [ ] **Step 5: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_store.py -v`
Expected: 4 passed

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/supernova_web/auth/models.py packages/web/src/supernova_web/auth/store.py packages/web/tests/test_auth_store.py
git commit -m "feat(web/auth): SQLite 存储层 + User/Session 模型 + 测试"
```

---

## Task 3: 会话管理（session.py）

**Files:**
- Create: `packages/web/src/supernova_web/auth/session.py`
- Test: `packages/web/tests/test_auth_session.py`

**Interfaces:**
- Consumes: `store.AuthStore`、`store.SessionRow`、`models.User`
- Produces: `SessionManager(store: AuthStore, ttl_hours: int = 12)`，方法：
  - `create(user_id: int) -> str`（返回新 session id，写入 store，过期 = now + ttl）
  - `verify(sid: str) -> User | None`（过期/不存在返回 None；有效则更新 last_seen_at 并返回 User）
  - `revoke(sid: str) -> None`
  - `purge_expired() -> int`

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_session.py
from supernova_web.auth.store import AuthStore
from supernova_web.auth.session import SessionManager


def _store(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    s.create_user("alice", "h")
    return s


def test_create_and_verify(tmp_path):
    s = _store(tmp_path)
    m = SessionManager(s, ttl_hours=12)
    sid = m.create(user_id=1)
    assert isinstance(sid, str) and len(sid) > 20
    user = m.verify(sid)
    assert user is not None and user.username == "alice"


def test_verify_unknown_returns_none(tmp_path):
    m = SessionManager(_store(tmp_path))
    assert m.verify("nope") is None


def test_expired_session_invalid(tmp_path):
    s = _store(tmp_path)
    m = SessionManager(s, ttl_hours=-1)  # 已过期
    sid = m.create(user_id=1)
    assert m.verify(sid) is None


def test_revoke(tmp_path):
    s = _store(tmp_path)
    m = SessionManager(s)
    sid = m.create(user_id=1)
    m.revoke(sid)
    assert m.verify(sid) is None


def test_purge_expired(tmp_path):
    s = _store(tmp_path)
    SessionManager(s, ttl_hours=-1).create(user_id=1)  # 过期
    m = SessionManager(s)
    assert m.purge_expired() == 1
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_session.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

```python
# packages/web/src/supernova_web/auth/session.py
from __future__ import annotations

import secrets
from datetime import datetime, timedelta, timezone

from .models import User
from .store import AuthStore, SessionRow


class SessionManager:
    def __init__(self, store: AuthStore, ttl_hours: int = 12) -> None:
        self.store = store
        self.ttl = timedelta(hours=ttl_hours)

    def create(self, user_id: int) -> str:
        sid = secrets.token_urlsafe(32)
        now = datetime.now(timezone.utc)
        self.store.insert_session(
            SessionRow(id=sid, user_id=user_id, expires_at=(now + self.ttl).isoformat(), last_seen_at=now.isoformat())
        )
        return sid

    def verify(self, sid: str) -> User | None:
        row = self.store.get_session(sid)
        if row is None:
            return None
        now = datetime.now(timezone.utc)
        try:
            exp = datetime.fromisoformat(row.expires_at)
        except ValueError:
            return None
        if exp < now:
            return None  # 过期视为未登录（惰性清理，不报错）
        self.store.update_session_seen(sid, now.isoformat())
        return self.store.get_user(row.user_id)

    def revoke(self, sid: str) -> None:
        self.store.delete_session(sid)

    def purge_expired(self) -> int:
        return self.store.purge_expired(datetime.now(timezone.utc).isoformat())
```

- [ ] **Step 4: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_session.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/supernova_web/auth/session.py packages/web/tests/test_auth_session.py
git commit -m "feat(web/auth): session 管理 (create/verify/revoke/purge) + 测试"
```

---

## Task 4: 预置账号 seed + hash 脚本（seed.py + web_hash_password.py + users.yaml.example）

**Files:**
- Create: `packages/web/src/supernova_web/auth/seed.py`
- Create: `scripts/web_hash_password.py`
- Create: `configs/users.yaml.example`
- Test: `packages/web/tests/test_auth_seed.py`

**Interfaces:**
- Consumes: `store.AuthStore`、`passwords.verify_password`（仅用于跳过判定，不重算 hash）
- Produces: `seed_users(store: AuthStore, yaml_path: str) -> int`（返回新建用户数；**不覆盖已存在用户**）；脚本独立可执行。

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_seed.py
from supernova_web.auth.store import AuthStore
from supernova_web.auth.seed import seed_users


def _yaml(tmp_path, body: str) -> str:
    p = tmp_path / "users.yaml"
    p.write_text(body, encoding="utf-8")
    return str(p)


def test_seed_creates_missing_users(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    path = _yaml(tmp_path, """
users:
  - username: admin
    password_hash: "$2b$12$aaa"
    role: admin
  - username: alice
    password_hash: "$2b$12$bbb"
""")
    n = seed_users(s, path)
    assert n == 2
    assert s.get_user_by_username("admin").role == "admin"
    assert s.get_user_by_username("alice").role == "user"  # 默认 user


def test_seed_does_not_overwrite_existing(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    s.create_user("admin", "$2b$12$OLD")  # 已存在，密码 OLD
    path = _yaml(tmp_path, """
users:
  - username: admin
    password_hash: "$2b$12$NEW"
""")
    n = seed_users(s, path)
    assert n == 0  # 没新建
    # 已存在用户不被改：重新读 password_hash 确认未被覆盖（store 不暴露 hash，用 get_user_by_username 仅确认仍在）
    assert s.get_user_by_username("admin") is not None


def test_seed_missing_file_is_noop(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    assert seed_users(s, str(tmp_path / "nope.yaml")) == 0


def test_seed_empty_users_key_is_noop(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    path = _yaml(tmp_path, "users: []\n")
    assert seed_users(s, path) == 0
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_seed.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现 seed.py**

```python
# packages/web/src/supernova_web/auth/seed.py
from __future__ import annotations

from pathlib import Path

from .store import AuthStore


def seed_users(store: AuthStore, yaml_path: str) -> int:
    """把 users.yaml 里不存在于 SQLite 的用户 upsert 进去；已存在用户不动（避免重启覆盖改过的密码）。
    yaml 缺失或 users 为空 → 0。返回新建用户数。"""
    p = Path(yaml_path)
    if not p.is_file():
        return 0
    try:
        import yaml  # PyYAML：core 已依赖，复用
    except ImportError:
        return 0
    data = yaml.safe_load(p.read_text(encoding="utf-8")) or {}
    users = data.get("users") or []
    created = 0
    for u in users:
        username = u.get("username")
        if not username:
            continue
        if store.get_user_by_username(username) is not None:
            continue  # 不覆盖
        store.create_user(username, u.get("password_hash", ""), role=u.get("role", "user"))
        created += 1
    return created
```

- [ ] **Step 4: 实现 web_hash_password.py**

```python
# scripts/web_hash_password.py
#!/usr/bin/env python3
"""交互式生成 bcrypt 密码 hash，粘进 configs/users.yaml 的 password_hash 字段。
用法: uv run python scripts/web_hash_password.py"""
from __future__ import annotations

import getpass
import sys

from passlib.context import CryptContext

_pwd = CryptContext(schemes=["bcrypt"], deprecated="auto")


def main() -> int:
    pw = getpass.getpass("密码: ")
    if not pw:
        print("密码不能为空", file=sys.stderr)
        return 1
    print("\n把下面这行粘进 users.yaml 的 password_hash：\n")
    print(_pwd.hash(pw))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 5: 实现 users.yaml.example**

```yaml
# configs/users.yaml.example — 拷成 configs/users.yaml 后改。password_hash 用
# `uv run python scripts/web_hash_password.py` 生成。users.yaml 含凭据勿入库（.gitignore 已忽略 configs/*.yaml）。
users:
  - username: admin
    password_hash: "$2b$12$REPLACE_WITH_GENERATED_HASH"
    role: admin
  - username: alice
    password_hash: "$2b$12$REPLACE_WITH_GENERATED_HASH"
    role: user
```

- [ ] **Step 6: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_seed.py -v`
Expected: 4 passed

- [ ] **Step 7: Commit**

```bash
git add packages/web/src/supernova_web/auth/seed.py scripts/web_hash_password.py configs/users.yaml.example packages/web/tests/test_auth_seed.py
git commit -m "feat(web/auth): 预置账号 seed (users.yaml→SQLite 不覆盖) + hash 脚本"
```

---

## Task 5: WebConfig auth 字段（config.py + .env.example）

**Files:**
- Modify: `packages/web/src/supernova_web/config.py`
- Modify: `.env.example`
- Test: `packages/web/tests/test_auth_config.py`

**Interfaces:**
- Produces: `WebConfig.session_ttl_hours:int`、`cookie_secure:bool`、`users_seed_file:str`、`auth_db_path:Path`（property，= `<workspaces_dir>/auth.db`）
- **决策（偏离 spec §5.4）**：spec 列的 `session_secret` 在 P0 砍掉（YAGNI）。session id 是 `secrets.token_urlsafe(32)` 随机串存 DB，cookie 直接存该 id（HttpOnly）——无需签名；DB 校验 + HttpOnly 已提供足够安全性 + 可即时撤销。未来若换签名 cookie 再加。

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_config.py
from supernova_web.config import get_config


def test_auth_defaults(tmp_workspaces):
    cfg = get_config()
    assert cfg.session_ttl_hours == 12
    assert cfg.cookie_secure is True
    assert cfg.users_seed_file == "configs/users.yaml"
    assert str(cfg.auth_db_path).endswith("auth.db")


def test_auth_env_override(tmp_workspaces, monkeypatch):
    monkeypatch.setenv("SUPERNOVA_WEB_SESSION_TTL_HOURS", "48")
    monkeypatch.setenv("SUPERNOVA_WEB_COOKIE_SECURE", "0")
    cfg = get_config()
    assert cfg.session_ttl_hours == 48
    assert cfg.cookie_secure is False
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_config.py -v`
Expected: FAIL（`AttributeError: session_ttl_hours`）

- [ ] **Step 3: 改 config.py**

在 `WebConfig.__init__` 末尾（`self.fs_roots = [...]` 之后）加：

```python
        # auth（P0）
        self.session_ttl_hours = int(os.environ.get("SUPERNOVA_WEB_SESSION_TTL_HOURS", "12"))
        self.cookie_secure = os.environ.get("SUPERNOVA_WEB_COOKIE_SECURE", "1") not in ("0", "false", "False")
        self.users_seed_file = os.environ.get("SUPERNOVA_WEB_USERS_SEED", "configs/users.yaml")
```

在 `workspaces_dir` property 之后加：

```python
    @property
    def auth_db_path(self) -> Path:
        return self.workspaces_dir / "auth.db"
```

- [ ] **Step 4: 改 .env.example**

在文件末尾追加：

```dotenv

# Web 认证（P0）
SUPERNOVA_WEB_SESSION_TTL_HOURS=12
SUPERNOVA_WEB_COOKIE_SECURE=1
SUPERNOVA_WEB_USERS_SEED=configs/users.yaml
```

- [ ] **Step 5: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_config.py -v`
Expected: 2 passed

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/supernova_web/config.py .env.example packages/web/tests/test_auth_config.py
git commit -m "feat(web/auth): WebConfig 加 auth 字段 (ttl/cookie_secure/seed/db_path)"
```

---

## Task 6: 登录暴力防护（brute.py）

**Files:**
- Create: `packages/web/src/supernova_web/auth/brute.py`
- Test: `packages/web/tests/test_auth_brute.py`

**Interfaces:**
- Consumes: 无
- Produces: `BruteGuard(threshold=5, lock_seconds=300)`，方法：`record_failure(username)`、`is_locked(username) -> bool`、`remaining(username) -> int`（剩余尝试次数）、`reset(username)`。**进程内存**（本地单 worker，P0 不持久化）。

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_brute.py
import time
from supernova_web.auth.brute import BruteGuard


def test_allows_before_threshold():
    g = BruteGuard(threshold=3, lock_seconds=60)
    for _ in range(2):
        g.record_failure("alice")
    assert not g.is_locked("alice")
    assert g.remaining("alice") == 1


def test_locks_at_threshold():
    g = BruteGuard(threshold=3, lock_seconds=60)
    for _ in range(3):
        g.record_failure("alice")
    assert g.is_locked("alice")
    assert g.remaining("alice") == 0


def test_reset_clears():
    g = BruteGuard(threshold=2, lock_seconds=60)
    g.record_failure("alice"); g.record_failure("alice")
    assert g.is_locked("alice")
    g.reset("alice")
    assert not g.is_locked("alice")
    assert g.remaining("alice") == 2


def test_unlocks_after_window(monkeypatch):
    t = {"v": 0.0}
    monkeypatch.setattr(time, "monotonic", lambda: t["v"])
    g = BruteGuard(threshold=1, lock_seconds=60)
    g.record_failure("alice")
    assert g.is_locked("alice")
    t["v"] += 61  # 过窗口
    assert not g.is_locked("alice")


def test_independent_per_user():
    g = BruteGuard(threshold=2, lock_seconds=60)
    g.record_failure("alice"); g.record_failure("alice")
    assert g.is_locked("alice")
    assert not g.is_locked("bob")
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_brute.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

```python
# packages/web/src/supernova_web/auth/brute.py
from __future__ import annotations

import time


class BruteGuard:
    """per-username 登录失败计数 + 临时锁定（进程内存）。本地单 worker 足够。"""

    def __init__(self, threshold: int = 5, lock_seconds: int = 300) -> None:
        self.threshold = threshold
        self.lock_seconds = lock_seconds
        self._fails: dict[str, int] = {}
        self._locked_until: dict[str, float] = {}

    def record_failure(self, username: str) -> None:
        self._fails[username] = self._fails.get(username, 0) + 1
        if self._fails[username] >= self.threshold:
            self._locked_until[username] = time.monotonic() + self.lock_seconds

    def is_locked(self, username: str) -> bool:
        until = self._locked_until.get(username)
        if until is None:
            return False
        if time.monotonic() >= until:
            # 窗口过，自动解锁 + 清计数
            self._locked_until.pop(username, None)
            self._fails.pop(username, None)
            return False
        return True

    def remaining(self, username: str) -> int:
        return max(0, self.threshold - self._fails.get(username, 0))

    def reset(self, username: str) -> None:
        self._fails.pop(username, None)
        self._locked_until.pop(username, None)
```

- [ ] **Step 4: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_brute.py -v`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/supernova_web/auth/brute.py packages/web/tests/test_auth_brute.py
git commit -m "feat(web/auth): 登录暴力防护 (per-username 失败计数+锁定)"
```

---

## Task 7: CSRF double-submit（csrf.py）

**Files:**
- Create: `packages/web/src/supernova_web/auth/csrf.py`
- Test: `packages/web/tests/test_auth_csrf.py`

**Interfaces:**
- Consumes: 无
- Produces: `generate_csrf_token() -> str`、`verify_csrf(header: str | None, cookie_value: str | None) -> bool`（两者非空且 `hmac.compare_digest` 相等）

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_csrf.py
from supernova_web.auth.csrf import generate_csrf_token, verify_csrf


def test_generate_unique():
    assert generate_csrf_token() != generate_csrf_token()
    assert len(generate_csrf_token()) > 20


def test_verify_match():
    tok = generate_csrf_token()
    assert verify_csrf(tok, tok) is True


def test_verify_mismatch():
    assert verify_csrf("a", "b") is False


def test_verify_missing():
    assert verify_csrf(None, "x") is False
    assert verify_csrf("x", None) is False
    assert verify_csrf(None, None) is False
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_csrf.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

```python
# packages/web/src/supernova_web/auth/csrf.py
from __future__ import annotations

import hmac
import secrets


def generate_csrf_token() -> str:
    return secrets.token_urlsafe(32)


def verify_csrf(header: str | None, cookie_value: str | None) -> bool:
    if not header or not cookie_value:
        return False
    return hmac.compare_digest(header, cookie_value)
```

- [ ] **Step 4: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_csrf.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/supernova_web/auth/csrf.py packages/web/tests/test_auth_csrf.py
git commit -m "feat(web/auth): CSRF double-submit token 生成/校验"
```

---

## Task 8: Auth middleware（cookie→session→user 注入）

**Files:**
- Create: `packages/web/src/supernova_web/auth/middleware.py`
- Test: `packages/web/tests/test_auth_middleware.py`

**Interfaces:**
- Consumes: `request.app.state.session_manager`（`SessionManager`，T10 在 create_app 里设）
- Produces: `AuthMiddleware`（starlette BaseHTTPMiddleware）——读 `sn-sid` cookie → `session_manager.verify(sid)` → 注入 `request.state.user`（`User | None`）。**不阻断请求**，只注入；由路由 `Depends(current_user)` 决定是否要求登录。

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_middleware.py
from fastapi import FastAPI
from starlette.testclient import TestClient

from supernova_web.auth.store import AuthStore
from supernova_web.auth.session import SessionManager
from supernova_web.auth.middleware import AuthMiddleware


def _app(tmp_path):
    store = AuthStore(str(tmp_path / "auth.db")); store.init_schema()
    store.create_user("alice", "h")
    sm = SessionManager(store)
    app = FastAPI()
    app.state.session_manager = sm
    app.add_middleware(AuthMiddleware)

    @app.get("/who")
    def who(request):
        u = getattr(request.state, "user", None)
        return {"user": u.username if u else None}

    return app, sm


def test_no_cookie_no_user(tmp_path):
    app, _ = _app(tmp_path)
    r = TestClient(app).get("/who")
    assert r.status_code == 200 and r.json() == {"user": None}


def test_valid_cookie_injects_user(tmp_path):
    app, sm = _app(tmp_path)
    sid = sm.create(user_id=1)
    c = TestClient(app); c.cookies.set("sn-sid", sid)
    r = c.get("/who")
    assert r.json() == {"user": "alice"}


def test_invalid_cookie_no_user(tmp_path):
    app, _ = _app(tmp_path)
    c = TestClient(app); c.cookies.set("sn-sid", "bogus")
    r = c.get("/who")
    assert r.json() == {"user": None}
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_middleware.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

```python
# packages/web/src/supernova_web/auth/middleware.py
from __future__ import annotations

from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request


class AuthMiddleware(BaseHTTPMiddleware):
    """读 sn-sid cookie → session_manager.verify → 注入 request.state.user。
    不阻断；路由用 Depends(current_user) 决定是否要求登录。"""

    async def dispatch(self, request: Request, call_next):
        sm = getattr(request.app.state, "session_manager", None)
        user = None
        if sm is not None:
            sid = request.cookies.get("sn-sid")
            if sid:
                user = sm.verify(sid)
        request.state.user = user
        return await call_next(request)
```

- [ ] **Step 4: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_middleware.py -v`
Expected: 3 passed

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/supernova_web/auth/middleware.py packages/web/tests/test_auth_middleware.py
git commit -m "feat(web/auth): auth middleware (cookie→session→request.state.user 注入)"
```

---

## Task 9: 鉴权依赖（dependencies.py）

**Files:**
- Create: `packages/web/src/supernova_web/auth/dependencies.py`
- Test: `packages/web/tests/test_auth_dependencies.py`

**Interfaces:**
- Consumes: `request.state.user`（由 AuthMiddleware 注入，`User | None`）
- Produces: `current_user(request) -> User`（None→401）、`require_admin(request) -> User`（非 admin→403）

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_dependencies.py
import pytest
from fastapi import HTTPException, Request

from supernova_web.auth.dependencies import current_user, require_admin
from supernova_web.auth.models import User


def _req(user):
    r = Request({"type": "http"})
    r.state.user = user
    return r


def test_current_user_ok():
    u = User(id=1, username="alice", role="user")
    assert current_user(_req(u)).username == "alice"


def test_current_user_unauthenticated():
    with pytest.raises(HTTPException) as e:
        current_user(_req(None))
    assert e.value.status_code == 401


def test_require_admin_ok():
    u = User(id=1, username="admin", role="admin")
    assert require_admin(_req(u)).role == "admin"


def test_require_admin_forbidden_for_user():
    u = User(id=1, username="alice", role="user")
    with pytest.raises(HTTPException) as e:
        require_admin(_req(u))
    assert e.value.status_code == 403
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_dependencies.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

```python
# packages/web/src/supernova_web/auth/dependencies.py
from __future__ import annotations

from fastapi import HTTPException, Request

from .models import User


def current_user(request: Request) -> User:
    user = getattr(request.state, "user", None)
    if user is None:
        raise HTTPException(status_code=401, detail="not authenticated")
    return user


def require_admin(request: Request) -> User:
    user = current_user(request)
    if user.role != "admin":
        raise HTTPException(status_code=403, detail="admin required")
    return user
```

- [ ] **Step 4: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_dependencies.py -v`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/supernova_web/auth/dependencies.py packages/web/tests/test_auth_dependencies.py
git commit -m "feat(web/auth): current_user/require_admin 依赖"
```

---

## Task 10: Auth 路由 + 注册到 app（routes.py + store 扩展 + app.py）

**Files:**
- Modify: `packages/web/src/supernova_web/auth/store.py`（加 `get_password_hash`）
- Modify: `packages/web/tests/test_auth_store.py`（加 `test_get_password_hash`）
- Create: `packages/web/src/supernova_web/auth/routes.py`
- Modify: `packages/web/src/supernova_web/app.py`（建 auth_store/session_manager + `add_middleware(AuthMiddleware)` + include auth router）
- Test: `packages/web/tests/test_auth_routes.py`

**Interfaces:**
- Consumes: T3 `SessionManager`、T1 `verify_password`、T6 `BruteGuard`、T7 `verify_csrf`/`generate_csrf_token`、T8 `AuthMiddleware`、T9 `current_user`、`request.app.state.{config, auth_store, session_manager}`
- Produces: `auth.routes.router`（prefix `/api/auth`：`GET /csrf`、`POST /login`、`POST /logout`、`GET /me`）；`store.AuthStore.get_password_hash(username) -> str | None`

- [ ] **Step 1: 扩展 store.get_password_hash + 测试**

在 `store.py` 的 `get_user_by_username` 方法之后加：

```python
    def get_password_hash(self, username: str) -> str | None:
        with self._conn() as c:
            row = c.execute(
                "SELECT password_hash FROM users WHERE username=?", (username,)
            ).fetchone()
        return row[0] if row else None
```

在 `test_auth_store.py` 加：

```python
def test_get_password_hash(tmp_path):
    s = AuthStore(str(tmp_path / "auth.db")); s.init_schema()
    s.create_user("alice", "$2b$12$xxx")
    assert s.get_password_hash("alice") == "$2b$12$xxx"
    assert s.get_password_hash("nobody") is None
```

Run: `uv run pytest packages/web/tests/test_auth_store.py -v` → 全 passed（含新 1 条）。

- [ ] **Step 2: 写 routes 失败测试**

```python
# packages/web/tests/test_auth_routes.py
import pytest
from starlette.testclient import TestClient

from supernova_web.app import create_app
from supernova_web.auth.passwords import hash_password


@pytest.fixture
def client(tmp_workspaces, monkeypatch):
    monkeypatch.setenv("SUPERNOVA_WEB_COOKIE_SECURE", "0")  # 测试走 HTTP
    app = create_app()
    app.state.auth_store.create_user("alice", hash_password("pw123"))
    return TestClient(app)


def _csrf(c):
    return c.get("/api/auth/csrf").json()["csrf_token"]


def test_login_success(client):
    tok = _csrf(client)
    r = client.post("/api/auth/login", json={"username": "alice", "password": "pw123"}, headers={"X-CSRF-Token": tok})
    assert r.status_code == 200
    assert r.json()["user"]["username"] == "alice"


def test_login_wrong_password(client):
    tok = _csrf(client)
    r = client.post("/api/auth/login", json={"username": "alice", "password": "wrong"}, headers={"X-CSRF-Token": tok})
    assert r.status_code == 401


def test_login_requires_csrf(client):
    r = client.post("/api/auth/login", json={"username": "alice", "password": "pw123"})
    assert r.status_code == 403


def test_me_after_login(client):
    tok = _csrf(client)
    client.post("/api/auth/login", json={"username": "alice", "password": "pw123"}, headers={"X-CSRF-Token": tok})
    r = client.get("/api/auth/me")
    assert r.status_code == 200 and r.json()["user"]["username"] == "alice"


def test_me_without_login_401(client):
    assert client.get("/api/auth/me").status_code == 401


def test_logout_invalidates_session(client):
    tok = _csrf(client)
    client.post("/api/auth/login", json={"username": "alice", "password": "pw123"}, headers={"X-CSRF-Token": tok})
    assert client.post("/api/auth/logout", headers={"X-CSRF-Token": tok}).status_code == 200
    assert client.get("/api/auth/me").status_code == 401
```

- [ ] **Step 3: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_routes.py -v`
Expected: FAIL（routes 未注册 / 404）

- [ ] **Step 4: 实现 routes.py**

```python
# packages/web/src/supernova_web/auth/routes.py
from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, Request
from fastapi.responses import JSONResponse
from pydantic import BaseModel

from .brute import BruteGuard
from .csrf import generate_csrf_token, verify_csrf
from .dependencies import current_user
from .models import User
from .passwords import verify_password

router = APIRouter(prefix="/api/auth", tags=["auth"])

_brute = BruteGuard()


class LoginIn(BaseModel):
    username: str
    password: str


def _user_out(u: User) -> dict:
    return {"id": u.id, "username": u.username, "role": u.role}


def _cookie_kwargs(cfg) -> dict:
    return {"httponly": True, "samesite": "lax", "secure": cfg.cookie_secure,
            "max_age": cfg.session_ttl_hours * 3600}


@router.get("/csrf")
def csrf(request: Request):
    cfg = request.app.state.config
    tok = generate_csrf_token()
    resp = JSONResponse({"csrf_token": tok})
    resp.set_cookie("sn-csrf", tok, httponly=False, samesite="lax", secure=cfg.cookie_secure)
    return resp


@router.post("/login")
def login(body: LoginIn, request: Request):
    cfg = request.app.state.config
    if not verify_csrf(request.headers.get("x-csrf-token"), request.cookies.get("sn-csrf")):
        raise HTTPException(status_code=403, detail="invalid csrf token")
    if _brute.is_locked(body.username):
        raise HTTPException(status_code=429, detail="too many attempts, try later")
    store = request.app.state.auth_store
    user = store.get_user_by_username(body.username)
    pw_hash = store.get_password_hash(body.username) if user else None
    ok = pw_hash is not None and verify_password(body.password, pw_hash)
    if not ok:
        _brute.record_failure(body.username)
        raise HTTPException(status_code=401, detail="invalid credentials")
    _brute.reset(body.username)
    sid = request.app.state.session_manager.create(user.id)
    resp = JSONResponse({"user": _user_out(user)})
    resp.set_cookie("sn-sid", sid, **_cookie_kwargs(cfg))
    tok = generate_csrf_token()  # 登录后续签 csrf
    resp.set_cookie("sn-csrf", tok, httponly=False, samesite="lax", secure=cfg.cookie_secure,
                    max_age=cfg.session_ttl_hours * 3600)
    return resp


@router.post("/logout")
def logout(request: Request):
    sid = request.cookies.get("sn-sid")
    if sid:
        request.app.state.session_manager.revoke(sid)
    resp = JSONResponse({"ok": True})
    resp.delete_cookie("sn-sid")
    resp.delete_cookie("sn-csrf")
    return resp


@router.get("/me")
def me(user: User = Depends(current_user)):
    return {"user": _user_out(user)}
```

- [ ] **Step 5: 注册到 app.py**

在 `create_app` 里，`app.state.config = cfg` 之后、现有 `WorkspacesIndexer` 之前插入：

```python
    from .auth.store import AuthStore
    from .auth.session import SessionManager
    from .auth.middleware import AuthMiddleware
    auth_store = AuthStore(str(cfg.auth_db_path))
    auth_store.init_schema()
    app.state.auth_store = auth_store
    app.state.session_manager = SessionManager(auth_store, ttl_hours=cfg.session_ttl_hours)
    app.add_middleware(AuthMiddleware)
```

在现有 `app.include_router(system_status.router)` 之后加：

```python
    from .auth import routes as auth_routes
    app.include_router(auth_routes.router)
```

- [ ] **Step 6: 验证通过**

Run: `uv run pytest packages/web/tests/test_auth_routes.py packages/web/tests/test_auth_store.py -v`
Expected: 全 passed

- [ ] **Step 7: Commit**

```bash
git add packages/web/src/supernova_web/auth/store.py packages/web/src/supernova_web/auth/routes.py packages/web/src/supernova_web/app.py packages/web/tests/test_auth_store.py packages/web/tests/test_auth_routes.py
git commit -m "feat(web/auth): auth 路由 (csrf/login/logout/me) + 注册 app + middleware"
```

---

## Task 11: 保护现有 /api/* 路由 + startup seed/purge

**Files:**
- Modify: `packages/web/src/supernova_web/app.py`（现有 include_router 加 `dependencies=[Depends(current_user)]`；lifespan startup seed + purge 后台任务）
- Modify: `packages/web/tests/conftest.py`（加 `authed_client` fixture）
- Test: `packages/web/tests/test_auth_protection.py`

**Interfaces:**
- Consumes: T9 `current_user`、T4 `seed_users`、T3 `SessionManager.purge_expired`
- Produces: 所有现有业务 `/api/*` 要求登录；`/health` 与 `/api/auth/*` 公开；conftest `authed_client` fixture（已登录的 TestClient，供现有测试迁移）

- [ ] **Step 1: 写失败测试**

```python
# packages/web/tests/test_auth_protection.py
import pytest
from starlette.testclient import TestClient

from supernova_web.app import create_app
from supernova_web.auth.passwords import hash_password


@pytest.fixture
def app_and_client(tmp_workspaces, monkeypatch):
    monkeypatch.setenv("SUPERNOVA_WEB_COOKIE_SECURE", "0")
    app = create_app()
    app.state.auth_store.create_user("alice", hash_password("pw123"))
    c = TestClient(app)
    return app, c


def _login(c):
    tok = c.get("/api/auth/csrf").json()["csrf_token"]
    c.post("/api/auth/login", json={"username": "alice", "password": "pw123"}, headers={"X-CSRF-Token": tok})


def test_protected_routes_require_auth(app_and_client):
    _, c = app_and_client
    for path in ["/api/workspaces", "/api/repos", "/api/multi-configs", "/api/system-status"]:
        assert c.get(path).status_code == 401, path


def test_health_public(app_and_client):
    _, c = app_and_client
    assert c.get("/health").status_code == 200


def test_authed_can_access(app_and_client):
    _, c = app_and_client
    _login(c)
    r = c.get("/api/workspaces")
    assert r.status_code != 401  # 登录后不再 401（可能 200 空列表）


def test_startup_seeds_users(tmp_path, tmp_workspaces, monkeypatch):
    yaml_path = tmp_path / "users.yaml"
    yaml_path.write_text(
        'users:\n  - username: admin\n    password_hash: "$2b$12$x"\n    role: admin\n',
        encoding="utf-8",
    )
    monkeypatch.setenv("SUPERNOVA_WEB_USERS_SEED", str(yaml_path))
    monkeypatch.setenv("SUPERNOVA_WEB_COOKIE_SECURE", "0")
    app = create_app()
    with TestClient(app):  # 触发 lifespan startup → seed
        pass
    assert app.state.auth_store.get_user_by_username("admin") is not None
```

- [ ] **Step 2: 验证失败**

Run: `uv run pytest packages/web/tests/test_auth_protection.py -v`
Expected: FAIL（现有路由未保护，返回非 401）

- [ ] **Step 3: 改 app.py — 保护现有路由**

把 `create_app` 里这一段：

```python
    app.include_router(workspaces.router)
    app.include_router(scan.router)
    app.include_router(multi_configs.router)
    app.include_router(repos.router)
    app.include_router(events.router)
    app.include_router(fs.router)
    app.include_router(system_status.router)
```

替换为：

```python
    from .auth.dependencies import current_user
    _require_auth = [Depends(current_user)]
    app.include_router(workspaces.router, dependencies=_require_auth)
    app.include_router(scan.router, dependencies=_require_auth)
    app.include_router(multi_configs.router, dependencies=_require_auth)
    app.include_router(repos.router, dependencies=_require_auth)
    app.include_router(events.router, dependencies=_require_auth)
    app.include_router(fs.router, dependencies=_require_auth)
    app.include_router(system_status.router, dependencies=_require_auth)
```

（`auth_routes.router` 与 `/health` 不加依赖，保持公开。）

- [ ] **Step 4: 改 app.py — lifespan startup seed + purge**

把 `lifespan` 改为（在 `app.state.repo_manager.migrate_legacy()` 之前插入 auth startup）：

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    # auth: 启动 seed 预置账号 + 周期清理过期 session
    from .auth.seed import seed_users
    import asyncio
    seed_users(app.state.auth_store, app.state.config.users_seed_file)

    async def _purge_loop():
        while True:
            try:
                app.state.session_manager.purge_expired()
            except Exception:
                pass
            await asyncio.sleep(3600)
    app.state._purge_task = asyncio.create_task(_purge_loop())

    app.state.repo_manager.migrate_legacy()
    await _reconcile_orphaned_scans(app)
    yield
    app.state._purge_task.cancel()
```

- [ ] **Step 5: 加 authed_client fixture（供现有测试迁移）**

在 `packages/web/tests/conftest.py` 末尾加：

```python
@pytest.fixture
def authed_client(app_with_ws, monkeypatch):
    """已登录的 TestClient（用户 tester/test-pw），供需要鉴权的现有测试迁移用。"""
    from starlette.testclient import TestClient
    from supernova_web.auth.passwords import hash_password
    monkeypatch.setenv("SUPERNOVA_WEB_COOKIE_SECURE", "0")
    app = app_with_ws
    app.state.auth_store.create_user("tester", hash_password("test-pw"))
    c = TestClient(app)
    tok = c.get("/api/auth/csrf").json()["csrf_token"]
    c.post("/api/auth/login", json={"username": "tester", "password": "test-pw"},
           headers={"X-CSRF-Token": tok})
    return c
```

- [ ] **Step 6: 验证新测试通过 + 适配现有测试**

Run: `uv run pytest packages/web/tests/test_auth_protection.py packages/web/tests/test_auth_*.py -v`
Expected: 全 passed

然后跑现有 web 测试找出因 401 失败的：
Run: `uv run pytest packages/web/tests/ -v 2>&1 | grep -E "401|FAIL"`
对失败的现有测试：把其 `TestClient(app)` 用法替换为 `authed_client` fixture（或对写操作补 `X-CSRF-Token` header）。**只改因鉴权失败的测试，不改业务逻辑。**

- [ ] **Step 7: Commit**

```bash
git add packages/web/src/supernova_web/app.py packages/web/tests/conftest.py packages/web/tests/test_auth_protection.py packages/web/tests/
git commit -m "feat(web/auth): 保护现有 /api/* (Depends current_user) + startup seed/purge + authed_client fixture"
```

---

## Task 12: 前端 fetch client 改造（credentials + CSRF + 401）

**Files:**
- Modify: `packages/web/frontend/src/api/client.ts`
- Test: `packages/web/frontend/src/api/client.test.ts`

**Interfaces:**
- Consumes: `sn-csrf` cookie（后端 `/api/auth/csrf` 设置）
- Produces: 所有请求带 `credentials: "include"`；写操作（POST/PUT/DELETE/PATCH）自动注入 `X-CSRF-Token`（从 `sn-csrf` cookie 读）；401 经 `onUnauthorized` 回调（默认跳 `/login?expired=1`），`opts.silent=true` 时静默（供 `/auth/me` 探测）。新增类型 `ReqOptions = { silent?: boolean; signal?: AbortSignal }`；`apiGet/apiPost/apiDelete` 新增可选 `opts` 末参（**向后兼容**，现有调用不改）。

- [ ] **Step 1: 写失败测试**

```ts
// packages/web/frontend/src/api/client.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { apiGet, apiPost, setUnauthorizedHandler } from "./client";

describe("client auth", () => {
  beforeEach(() => {
    vi.restoreAllMocks();
    document.cookie = "sn-csrf=token123; path=/";
  });

  it("sends credentials: include", async () => {
    const fm = vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 200 }));
    await apiGet("/foo");
    expect(fm.mock.calls[0][1]).toMatchObject({ credentials: "include" });
  });

  it("adds X-CSRF-Token on POST", async () => {
    const fm = vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 200 }));
    await apiPost("/foo", { a: 1 });
    const init = fm.mock.calls[0][1] as RequestInit;
    expect((init.headers as Record<string, string>)["X-CSRF-Token"]).toBe("token123");
  });

  it("401 silent does not fire handler", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 401 }));
    const h = vi.fn();
    setUnauthorizedHandler(h);
    await expect(apiGet("/me", { silent: true })).rejects.toThrow();
    expect(h).not.toHaveBeenCalled();
  });

  it("401 non-silent fires handler", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 401 }));
    const h = vi.fn();
    setUnauthorizedHandler(h);
    await expect(apiGet("/workspaces")).rejects.toThrow();
    expect(h).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/api/client.test.ts`
Expected: FAIL（`credentials` 未带 / handler 未触发）

- [ ] **Step 3: 改 client.ts**

把 `client.ts` 整体替换为（保留所有现有业务函数，只升级基础设施）：

```ts
// packages/web/frontend/src/api/client.ts
import type { FsBrowseResult, Repo, RepoDetail } from "./types";

export class ApiError extends Error {
  constructor(public status: number, public body: unknown) {
    super(`API ${status}`);
    this.name = "ApiError";
  }
}

export type ReqOptions = { silent?: boolean; signal?: AbortSignal };

let onUnauthorized: () => void = () => window.location.assign("/login?expired=1");
export function setUnauthorizedHandler(fn: () => void) {
  onUnauthorized = fn;
}

function readCookie(name: string): string | null {
  const m = document.cookie.match(
    new RegExp("(?:^|; )" + name.replace(/([.$?*|{}()[\]\\/+^])/g, "\\$1") + "=([^;]*)")
  );
  return m ? decodeURIComponent(m[1]) : null;
}

const WRITE_METHODS = new Set(["POST", "PUT", "PATCH", "DELETE"]);

async function request<T>(path: string, init?: RequestInit, opts?: ReqOptions): Promise<T> {
  const method = init?.method?.toUpperCase();
  const headers: Record<string, string> = { "Content-Type": "application/json", ...(init?.headers as Record<string, string> ?? {}) };
  if (method && WRITE_METHODS.has(method)) {
    const tok = readCookie("sn-csrf");
    if (tok) headers["X-CSRF-Token"] = tok;
  }
  const res = await fetch(`/api${path}`, {
    ...init,
    headers,
    credentials: "include",
    signal: opts?.signal,
  });
  if (!res.ok) {
    if (res.status === 401 && !opts?.silent) onUnauthorized();
    let body: unknown;
    try {
      body = await res.json();
    } catch {
      body = await res.text();
    }
    throw new ApiError(res.status, body);
  }
  const text = await res.text();
  return (text ? JSON.parse(text) : {}) as T;
}

export const apiGet = <T>(path: string, opts?: ReqOptions) => request<T>(path, undefined, opts);
export const apiPost = <T>(path: string, body: unknown, opts?: ReqOptions) =>
  request<T>(path, { method: "POST", body: JSON.stringify(body) }, opts);
export const apiDelete = <T>(path: string, opts?: ReqOptions) =>
  request<T>(path, { method: "DELETE" }, opts);

export const browseFs = (path: string) =>
  apiGet<FsBrowseResult>(`/fs/browse?path=${encodeURIComponent(path)}`);
export const deleteWorkspace = (ws: string) =>
  apiDelete<{ deleted: string }>(`/workspaces/${encodeURIComponent(ws)}`);
export type CancelResult = { cancelled: string; via?: string; was_dead?: boolean };
export const cancelScan = (ws: string) =>
  apiDelete<CancelResult>(`/scan/${encodeURIComponent(ws)}`);

const encRepo = (name: string) => name.split("/").map(encodeURIComponent).join("/");

export const listRepos = () => apiGet<Repo[]>("/repos");
export const getRepo = (name: string) => apiGet<RepoDetail>(`/repos/${encRepo(name)}`);
export const createRepo = (body: { git_url: string; branch?: string; commit?: string; name?: string; group?: string }) =>
  apiPost<{ name: string }>("/repos", body);
export const deleteRepo = (name: string) =>
  apiDelete<{ deleted: string }>(`/repos/${encRepo(name)}`);
export const pullRepo = (name: string) =>
  apiPost<{ pulling: string }>(`/repos/${encRepo(name)}/pull`, {});
export const checkoutRepo = (name: string, branch: string) =>
  apiPost<{ checked_out: string }>(`/repos/${encRepo(name)}/checkout`, { branch });

export async function apiGetText(path: string): Promise<string> {
  const res = await fetch(`/api${path}`, { credentials: "include" });
  if (!res.ok) throw new ApiError(res.status, await res.text());
  return res.text();
}
```

- [ ] **Step 4: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/api/client.test.ts`
Expected: 4 passed

- [ ] **Step 5: Commit**

```bash
cd packages/web/frontend
git add src/api/client.ts src/api/client.test.ts
git commit -m "feat(web/auth): fetch client 加 credentials+CSRF header+401 处理 (向后兼容)"
```

---

## Task 13: AuthContext + useAuth

**Files:**
- Create: `packages/web/frontend/src/auth/AuthContext.tsx`
- Test: `packages/web/frontend/src/auth/AuthContext.test.tsx`

**Interfaces:**
- Consumes: `apiGet`/`apiPost`（T12）
- Produces: `AuthProvider`、`useAuth()` → `{ user: AuthUser | null; loading: boolean; login(u,p); logout() }`；`AuthUser = { id; username; role }`

- [ ] **Step 1: 写失败测试**

```tsx
// packages/web/frontend/src/auth/AuthContext.test.tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { render, screen, waitFor, act } from "@testing-library/react";
import { AuthProvider, useAuth } from "./AuthContext";

function ShowUser() {
  const { user, loading } = useAuth();
  return <div>{loading ? "loading" : user ? `user:${user.username}` : "anon"}</div>;
}

beforeEach(() => vi.restoreAllMocks());

describe("AuthContext", () => {
  it("mounts anonymous when /me returns 401", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 401 }));
    render(<AuthProvider><ShowUser /></AuthProvider>);
    await waitFor(() => expect(screen.getByText("anon")).toBeTruthy());
  });

  it("mounts logged-in when /me returns user", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(
      new Response(JSON.stringify({ user: { id: 1, username: "alice", role: "user" } }), { status: 200 })
    );
    render(<AuthProvider><ShowUser /></AuthProvider>);
    await waitFor(() => expect(screen.getByText("user:alice")).toBeTruthy());
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/auth/AuthContext.test.tsx`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现**

```tsx
// packages/web/frontend/src/auth/AuthContext.tsx
import { createContext, useContext, useEffect, useState } from "react";
import type { ReactNode } from "react";
import { apiGet, apiPost } from "@/api/client";

export type AuthUser = { id: number; username: string; role: string };
type AuthState = {
  user: AuthUser | null;
  loading: boolean;
  login: (username: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
};

const Ctx = createContext<AuthState | null>(null);

export function AuthProvider({ children }: { children: ReactNode }) {
  const [user, setUser] = useState<AuthUser | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    apiGet<{ user: AuthUser }>("/auth/me", { silent: true })
      .then((r) => setUser(r.user))
      .catch(() => setUser(null))
      .finally(() => setLoading(false));
  }, []);

  async function login(username: string, password: string) {
    await apiGet("/auth/csrf");  // 拿 sn-csrf cookie（写操作 csrf header 由 client 自动注入）
    const r = await apiPost<{ user: AuthUser }>("/auth/login", { username, password });
    setUser(r.user);
  }

  async function logout() {
    await apiPost("/auth/logout", {});
    setUser(null);
  }

  return <Ctx.Provider value={{ user, loading, login, logout }}>{children}</Ctx.Provider>;
}

export function useAuth(): AuthState {
  const v = useContext(Ctx);
  if (!v) throw new Error("useAuth 必须在 AuthProvider 内使用");
  return v;
}
```

- [ ] **Step 4: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/auth/AuthContext.test.tsx`
Expected: 2 passed

- [ ] **Step 5: Commit**

```bash
cd packages/web/frontend
git add src/auth/AuthContext.tsx src/auth/AuthContext.test.tsx
git commit -m "feat(web/auth): AuthProvider + useAuth (me 探测/login/logout)"
```

---

## Task 14: i18n auth.* 文案（zh + en）

**Files:**
- Modify: `packages/web/frontend/src/locales/zh.json`（顶层加 `auth` 分组）
- Modify: `packages/web/frontend/src/locales/en.json`（同结构英文）
- Test: `packages/web/frontend/src/locales/locales.test.ts`

**Interfaces:** 产出 `auth.{tagline, login.{title,username,password,submit,invalid,welcome,sub}, logout, sessionExpired, sessionTtlHint, role.{admin,user}}`。

- [ ] **Step 1: 写失败测试**

```ts
// packages/web/frontend/src/locales/locales.test.ts
import { describe, it, expect } from "vitest";
import zh from "./zh.json";
import en from "./en.json";

describe("auth i18n", () => {
  it("zh.auth.login.submit 是真中文", () => {
    expect((zh as any).auth.login.submit).toBe("登录");
  });
  it("zh.auth.role.admin 是真中文（守 zh 值漏翻陷阱）", () => {
    expect((zh as any).auth.role.admin).toBe("管理员");
    expect((zh as any).auth.role.user).toBe("用户");
  });
  it("zh.auth.sessionExpired 真中文", () => {
    expect((zh as any).auth.sessionExpired).toBe("会话已过期，请重新登录");
  });
  it("en has auth keys", () => {
    expect((en as any).auth.login.submit).toBe("Sign in");
    expect((en as any).auth.role.admin).toBe("Admin");
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/locales/locales.test.ts`
Expected: FAIL（`auth` undefined）

- [ ] **Step 3: 改 zh.json**

在顶层对象里加一个 `auth` 键（与 `common`/`nav` 同级）：

```json
"auth": {
  "tagline": "白盒 · 黑盒 安全审计",
  "login": {
    "title": "登录 Supernova",
    "username": "用户名",
    "password": "密码",
    "submit": "登录",
    "invalid": "用户名或密码错误",
    "welcome": "欢迎回来",
    "sub": "登录以继续"
  },
  "logout": "登出",
  "sessionExpired": "会话已过期，请重新登录",
  "sessionTtlHint": "会话有效期 12 小时",
  "role": { "admin": "管理员", "user": "用户" }
},
```

- [ ] **Step 4: 改 en.json**

加同结构英文（注意逗号位置同 zh）：

```json
"auth": {
  "tagline": "White-box · Black-box Security Audit",
  "login": {
    "title": "Sign in to Supernova",
    "username": "Username",
    "password": "Password",
    "submit": "Sign in",
    "invalid": "Invalid username or password",
    "welcome": "Welcome back",
    "sub": "Sign in to continue"
  },
  "logout": "Sign out",
  "sessionExpired": "Session expired, please sign in again",
  "sessionTtlHint": "Session valid for 12 hours",
  "role": { "admin": "Admin", "user": "User" }
},
```

- [ ] **Step 5: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/locales/locales.test.ts`
Expected: 4 passed

- [ ] **Step 6: Commit**

```bash
cd packages/web/frontend
git add src/locales/zh.json src/locales/en.json src/locales/locales.test.ts
git commit -m "feat(web/auth): i18n auth.* 文案 (zh 真中文 + en)"
```

---

## Task 15: 登录页（claude 风分屏）+ /login 路由 + App 包 AuthProvider

**Files:**
- Create: `packages/web/frontend/src/pages/LoginPage.tsx`
- Modify: `packages/web/frontend/src/router.tsx`（加 `/login` 公开路由）
- Modify: `packages/web/frontend/src/App.tsx`（包 `AuthProvider`）
- Test: `packages/web/frontend/src/pages/LoginPage.test.tsx`

**Interfaces:**
- Consumes: T13 `useAuth`、T14 `auth.*` 文案、shadcn `Input`/`Label`/`Button`、DSF token（强制 `.light`）
- Produces: 默认导出 `LoginPage`（claude 风分屏：左 45% 暖深渐变 + ✦ logo + Supernova + 标语；右 55% 表单）。已登录 → `<Navigate to="/">`。提交成功 → 跳 `?next=`。

- [ ] **Step 1: 写失败测试**

```tsx
// packages/web/frontend/src/pages/LoginPage.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import { MemoryRouter, Routes, Route } from "react-router-dom";
import { AuthProvider } from "@/auth/AuthContext";
import LoginPage from "./LoginPage";

function wrap() {
  return render(
    <AuthProvider>
      <MemoryRouter initialEntries={["/login"]}>
        <Routes><Route path="/login" element={<LoginPage />} /></Routes>
      </MemoryRouter>
    </AuthProvider>
  );
}

describe("LoginPage", () => {
  it("渲染欢迎标题与表单（未登录态）", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 401 })); // /me 401
    wrap();
    await waitFor(() => expect(screen.getByText("欢迎回来")).toBeTruthy());
    expect(screen.getByLabelText("用户名")).toBeTruthy();
    expect(screen.getByLabelText("密码")).toBeTruthy();
    expect(screen.getByRole("button", { name: "登录" })).toBeTruthy();
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/pages/LoginPage.test.tsx`
Expected: FAIL（`LoginPage` 不存在）

- [ ] **Step 3: 实现 LoginPage.tsx**

```tsx
// packages/web/frontend/src/pages/LoginPage.tsx
import { useState } from "react";
import type { FormEvent } from "react";
import { Navigate, useNavigate, useSearchParams } from "react-router-dom";
import { useTranslation } from "react-i18next";
import { useAuth } from "@/auth/AuthContext";
import { ApiError } from "@/api/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

const SERIF = { fontFamily: "var(--font-serif), Georgia, serif" } as const;
const GRADIENT = "linear-gradient(155deg, #2E2520 0%, #4A2E22 55%, #6B3A26 100%)";

export default function LoginPage() {
  const { t } = useTranslation();
  const { user, login } = useAuth();
  const nav = useNavigate();
  const [params] = useSearchParams();
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  if (user) return <Navigate to="/" replace />;
  const expired = params.get("expired") === "1";
  const next = params.get("next") || "/";

  async function onSubmit(e: FormEvent) {
    e.preventDefault();
    setBusy(true); setError(null);
    try {
      await login(username, password);
      nav(next, { replace: true });
    } catch (err) {
      setError(err instanceof ApiError && err.status === 401 ? t("auth.login.invalid") : "Login failed");
    } finally {
      setBusy(false);
    }
  }

  return (
    <div className="light flex min-h-screen">
      {/* 左品牌区 */}
      <div
        className="hidden flex-col items-center justify-center gap-3 text-white md:flex md:w-[45%]"
        style={{ background: GRADIENT }}
      >
        <div
          className="flex h-14 w-14 items-center justify-center rounded-full bg-[#D97757] text-2xl"
          style={{ boxShadow: "0 0 0 8px rgba(217,119,87,0.18)" }}
        >
          ✦
        </div>
        <div className="text-2xl font-semibold tracking-tight" style={SERIF}>Supernova</div>
        <div className="text-sm opacity-70">{t("auth.tagline")}</div>
      </div>
      {/* 右表单 */}
      <div className="flex flex-1 items-center justify-center bg-background p-6">
        <form onSubmit={onSubmit} className="w-full max-w-sm space-y-4">
          <div className="space-y-1">
            <h1 className="text-2xl font-semibold text-foreground" style={SERIF}>
              {t("auth.login.welcome")}
            </h1>
            <p className="text-sm text-muted-foreground">{t("auth.login.sub")}</p>
          </div>
          {expired && <p className="text-sm text-destructive">{t("auth.sessionExpired")}</p>}
          {error && <p role="alert" className="text-sm text-destructive">{error}</p>}
          <div className="space-y-2">
            <Label htmlFor="u">{t("auth.login.username")}</Label>
            <Input id="u" value={username} onChange={(e) => setUsername(e.target.value)} autoComplete="username" required />
          </div>
          <div className="space-y-2">
            <Label htmlFor="p">{t("auth.login.password")}</Label>
            <Input id="p" type="password" value={password} onChange={(e) => setPassword(e.target.value)} autoComplete="current-password" required />
          </div>
          <Button type="submit" className="w-full" disabled={busy}>
            {busy ? "…" : t("auth.login.submit")}
          </Button>
          <p className="text-center text-xs text-muted-foreground">{t("auth.sessionTtlHint")}</p>
        </form>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 改 router.tsx — 加 /login 路由**

在 `router.tsx` 顶部 import 加：

```tsx
import LoginPage from "./pages/LoginPage";
```

把 `createBrowserRouter([...])` 改为在最前面加一条公开 `/login`：

```tsx
export const router = createBrowserRouter([
  { path: "/login", element: <LoginPage /> },
  {
    element: <AppShell />,
    children: [
      { path: "/", element: <DashboardPage /> },
      // ...其余业务路由保持不变...
    ],
  },
]);
```

（业务路由的 `RequireAuth` 包裹在 Task 16 加。）

- [ ] **Step 5: 改 App.tsx — 包 AuthProvider**

```tsx
// packages/web/frontend/src/App.tsx
import { RouterProvider } from "react-router-dom";
import { router } from "./router";
import { Toaster } from "@/components/ui/sonner";
import { BrandProvider } from "@/brand/BrandContext";
import { AuthProvider } from "./auth/AuthContext";

export default function App() {
  return (
    <BrandProvider>
      <AuthProvider>
        <RouterProvider router={router} />
      </AuthProvider>
      <Toaster />
    </BrandProvider>
  );
}
```

- [ ] **Step 6: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/pages/LoginPage.test.tsx`
Expected: 1 passed

- [ ] **Step 7: Commit**

```bash
cd packages/web/frontend
git add src/pages/LoginPage.tsx src/router.tsx src/App.tsx src/pages/LoginPage.test.tsx
git commit -m "feat(web/auth): claude 风分屏登录页 + /login 路由 + App 包 AuthProvider"
```

---

## Task 16: 路由守卫 RequireAuth + 业务路由保护

**Files:**
- Create: `packages/web/frontend/src/auth/RequireAuth.tsx`
- Modify: `packages/web/frontend/src/router.tsx`（业务路由 `element` 包 `RequireAuth`）
- Test: `packages/web/frontend/src/auth/RequireAuth.test.tsx`

**Interfaces:**
- Consumes: T13 `useAuth`
- Produces: `RequireAuth({children})`——`loading`→Loading 屏；`!user`→`<Navigate to="/login?next=...">`；否则渲染 children。

- [ ] **Step 1: 写失败测试**

```tsx
// packages/web/frontend/src/auth/RequireAuth.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import { MemoryRouter, Routes, Route } from "react-router-dom";
import { AuthProvider } from "@/auth/AuthContext";
import { RequireAuth } from "./RequireAuth";

function Protected() {
  return <div>protected content</div>;
}

function wrap(initial = "/secret") {
  return render(
    <AuthProvider>
      <MemoryRouter initialEntries={[initial]}>
        <Routes>
          <Route path="/login" element={<div>login page</div>} />
          <Route path={initial} element={<RequireAuth><Protected /></RequireAuth>} />
        </Routes>
      </MemoryRouter>
    </AuthProvider>
  );
}

describe("RequireAuth", () => {
  it("未登录跳 /login", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(new Response("{}", { status: 401 }));
    wrap();
    await waitFor(() => expect(screen.getByText("login page")).toBeTruthy());
    expect(screen.queryByText("protected content")).toBeNull();
  });

  it("loading 时显示 Loading 屏", () => {
    vi.spyOn(window, "fetch").mockImplementation(() => new Promise(() => {})); // 永久 pending
    wrap();
    expect(screen.getByText(/Loading/i)).toBeTruthy();
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/auth/RequireAuth.test.tsx`
Expected: FAIL（`RequireAuth` 不存在）

- [ ] **Step 3: 实现 RequireAuth.tsx**

```tsx
// packages/web/frontend/src/auth/RequireAuth.tsx
import { Navigate, useLocation } from "react-router-dom";
import type { ReactNode } from "react";
import { useAuth } from "./AuthContext";

export function RequireAuth({ children }: { children: ReactNode }) {
  const { user, loading } = useAuth();
  const loc = useLocation();
  if (loading) {
    return <div className="flex min-h-screen items-center justify-center text-muted-foreground">Loading…</div>;
  }
  if (!user) {
    return <Navigate to={`/login?next=${encodeURIComponent(loc.pathname + loc.search)}`} replace />;
  }
  return <>{children}</>;
}
```

- [ ] **Step 4: 改 router.tsx — 业务路由包 RequireAuth**

import 加 `import { RequireAuth } from "./auth/RequireAuth";`

把业务路由组的 `element: <AppShell />` 改为：

```tsx
    element: <RequireAuth><AppShell /></RequireAuth>,
```

（`/login` 公开路由保持不变。AppShell 内 `<Outlet/>` 渲染子路由不变。）

- [ ] **Step 5: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/auth/RequireAuth.test.tsx`
Expected: 2 passed

- [ ] **Step 6: Commit**

```bash
cd packages/web/frontend
git add src/auth/RequireAuth.tsx src/router.tsx src/auth/RequireAuth.test.tsx
git commit -m "feat(web/auth): RequireAuth 路由守卫 + 业务路由保护"
```

---

## Task 17: 用户菜单 UserMenu + TopBar 集成

**Files:**
- Create: `packages/web/frontend/src/components/layout/UserMenu.tsx`
- Modify: `packages/web/frontend/src/components/layout/TopBar.tsx`（右侧加 `<UserMenu />`）
- Test: `packages/web/frontend/src/components/layout/UserMenu.test.tsx`

**Interfaces:**
- Consumes: T13 `useAuth`、T14 `auth.role.*`/`auth.logout`、shadcn `Popover`
- Produces: `<UserMenu />`——已登录才渲染；头像首字母 + role badge（`admin` 暖橙 `--c-orange`、`user` 中性）；Popover 下拉显示用户名/角色 + 登出按钮。

- [ ] **Step 1: 写失败测试**

```tsx
// packages/web/frontend/src/components/layout/UserMenu.test.tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen, waitFor } from "@testing-library/react";
import { AuthProvider } from "@/auth/AuthContext";
import { UserMenu } from "./UserMenu";

vi.mock("react-i18next", () => ({
  useTranslation: () => ({ t: (k: string) => k }),
}));

describe("UserMenu", () => {
  it("登录后渲染首字母", async () => {
    vi.spyOn(window, "fetch").mockResolvedValue(
      new Response(JSON.stringify({ user: { id: 1, username: "alice", role: "user" } }), { status: 200 })
    );
    render(<AuthProvider><UserMenu /></AuthProvider>);
    await waitFor(() => expect(screen.getByText("A")).toBeTruthy()); // alice 首字母
  });
});
```

- [ ] **Step 2: 验证失败**

Run: `cd packages/web/frontend && npx vitest run src/components/layout/UserMenu.test.tsx`
Expected: FAIL（`UserMenu` 不存在）

- [ ] **Step 3: 实现 UserMenu.tsx**

```tsx
// packages/web/frontend/src/components/layout/UserMenu.tsx
import { useState } from "react";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { Button } from "@/components/ui/button";
import { useTranslation } from "react-i18next";
import { useAuth } from "@/auth/AuthContext";
import { cn } from "@/lib/utils";

export function UserMenu() {
  const { user, logout } = useAuth();
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  if (!user) return null;
  const isAdmin = user.role === "admin";
  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <button className="flex items-center gap-1.5 rounded-md px-2 py-1 text-sm hover:bg-accent" data-testid="user-menu-trigger">
          <span className="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-xs text-primary-foreground">
            {user.username.charAt(0).toUpperCase()}
          </span>
          <span className={cn("rounded px-1.5 py-0.5 text-xs", isAdmin ? "text-[hsl(var(--c-orange))]" : "text-muted-foreground")}>
            {t(`auth.role.${user.role}`)}
          </span>
        </button>
      </PopoverTrigger>
      <PopoverContent align="end" className="w-48">
        <div className="px-3 py-2 text-sm">
          <div className="font-medium">{user.username}</div>
          <div className="text-xs text-muted-foreground">{t(`auth.role.${user.role}`)}</div>
        </div>
        <Button
          variant="ghost"
          className="w-full justify-start"
          onClick={() => {
            setOpen(false);
            void logout();
          }}
        >
          {t("auth.logout")}
        </Button>
      </PopoverContent>
    </Popover>
  );
}
```

- [ ] **Step 4: 改 TopBar.tsx — 加 UserMenu**

import 加 `import { UserMenu } from "./UserMenu";`

把 TopBar 右侧：

```tsx
        <div className="ml-auto flex items-center gap-1">
          {/* 运行中扫描指示器 slot（子项目 5 接 SSE） */}
          <LanguageSwitcher />
          <ThemeToggle />
        </div>
```

改为：

```tsx
        <div className="ml-auto flex items-center gap-1">
          {/* 运行中扫描指示器 slot（子项目 5 接 SSE） */}
          <LanguageSwitcher />
          <ThemeToggle />
          <UserMenu />
        </div>
```

**注意**：TopBar 现有测试若直接 render `<TopBar/>`，因 UserMenu 用 `useAuth` 会报"不在 AuthProvider 内"。把这些测试用 `<AuthProvider>` 包裹，或 vi.mock `@/auth/AuthContext`。仅改测试装配，不改业务逻辑。

- [ ] **Step 5: 验证通过**

Run: `cd packages/web/frontend && npx vitest run src/components/layout/UserMenu.test.tsx`
Expected: 1 passed

并跑 TopBar 相关现有测试，按上面注意点适配因 `useAuth` 失败的：
Run: `cd packages/web/frontend && npx vitest run src/components/layout 2>&1 | grep -E "FAIL|AuthProvider"`

- [ ] **Step 6: Commit**

```bash
cd packages/web/frontend
git add src/components/layout/UserMenu.tsx src/components/layout/UserMenu.test.tsx src/components/layout/TopBar.tsx
git commit -m "feat(web/auth): UserMenu (Popover) + TopBar 集成 用户菜单/登出"
```

---

## Task 18: 端到端冒烟（真机）

**Files:** 无（手动验证）。**前置**：T1–T17 全绿，前端 `npx vitest run` 全绿、后端 `uv run pytest packages/web/tests/test_auth_*.py` 全绿、`cd packages/web/frontend && npm run build` 通过。

- [ ] **Step 1: 准备预置账号**

```bash
cd /Users/mango/project/shannon-refactor/shannon-py
# 生成 admin 密码 hash
uv run python scripts/web_hash_password.py   # 输入密码，复制 hash
# 写 configs/users.yaml（configs/*.yaml 已 gitignore）
cat > configs/users.yaml <<'EOF'
users:
  - username: admin
    password_hash: "粘上面生成的 hash"
    role: admin
EOF
```

- [ ] **Step 2: 起 web（本地 dev：后端 + 前端 vite proxy）**

```bash
# 后端
SUPERNOVA_PROFILE=glm-openai uv run supernova-web   # 或 docker compose up -d web worker
# 前端 dev（另一终端）
cd packages/web/frontend && npm run dev   # vite :5173，proxy /api → :7878
```

- [ ] **Step 3: 冒烟 checklist（浏览器 :5173）**

- [ ] 访问 `/` → 自动跳 `/login?next=%2F`
- [ ] 登录页是 **claude 风分屏**：左暖深渐变 + ✦ + Supernova + 标语，右表单
- [ ] 输错密码 → 显示「用户名或密码错误」，不跳转
- [ ] 输对密码 → 跳回 `/`（Dashboard），TopBar 右侧出现**用户菜单**（A + admin 暖橙 badge）
- [ ] 点用户菜单 → 「登出」→ 跳 `/login`
- [ ] 模拟会话过期：DevTools 清 `sn-sid` cookie 后刷新 → 跳 `/login?expired=1`，显示「会话已过期，请重新登录」
- [ ] curl 验证 API 鉴权：`curl -i http://localhost:7878/api/workspaces` → `401`

- [ ] **Step 4: 记录结果**

在 PR/commit 描述记录冒烟通过项与截图。若有项失败，回到对应 task 修复（不在本计划新增 task）。

---

## Self-Review

**1. Spec coverage（spec § → task）**
- §4.1 凭证方案 A（server session+cookie+SQLite）→ T2/T3/T8/T10 ✓
- §4.2 组件清单 → T1–T11（后端）、T12–T17（前端）逐文件覆盖 ✓
- §4.3 数据流（登录/鉴权/登出/过期）→ T10（login/logout/me）、T8（middleware 注入）、T11（startup purge）、T16（前端跳转）、T18（过期场景）✓
- §5.1 SQLite schema（users/sessions/schema_meta）→ T2 ✓
- §5.2 模块职责 → T1–T11 一一对应 ✓
- §5.3 路由清单 → T10 + T11 ✓
- §5.4 WebConfig 字段 → T5（注：`session_secret` 按 §决策 YAGNI 砍除，已注明）✓
- §5.5 预置账号 → T4（seed+脚本+example）+ T11（startup seed）+ T18（真机）✓
- §5.6 依赖 passlib[bcrypt] → T1 ✓
- §6 前端非视觉（AuthProvider/RequireAuth/fetch/TopBar/i18n）→ T12–T17 ✓
- §7 登录页视觉（B 分屏 + B1 + 固定亮色 + serif + 暖橙）→ T15（`.light` 根 + GRADIENT + ✦ + `--font-serif` + `--c-orange`）✓
- §8 安全清单（cookie flags/CSRF/bcrypt/session/暴力防护）→ T6(brute)/T7(csrf)/T8(cookie HttpOnly)/T10(SameSite/Secure)/T1(bcrypt) ✓
- §9 测试策略 → 每 task TDD + T18 冒烟 ✓
- §10 范围边界 → Global Constraints 明确"不碰 P1/P2/P3c"✓

**2. Placeholder scan**：无 TBD/TODO/"适当处理"等；每步含真实代码。

**3. Type consistency**：跨 task 核对一致——
- `User(id, username, role)`：T2 定义 → T3/T9/T10 一致 ✓
- `SessionManager.create(user_id)->str` / `verify(sid)->User|None` / `revoke/purge_expired`：T3 定义 → T8/T10 一致 ✓
- `AuthStore.get_password_hash`：T10 定义（扩展 store）→ 仅 T10 login 使用 ✓
- 前端 `AuthUser={id,username,role}`：T13 定义 → T15/T17 一致 ✓
- `ReqOptions={silent?,signal?}`：T12 定义 → T13(`{silent:true}`) 一致 ✓
- cookie 名 `sn-sid`/`sn-csrf`：T8/T10（后端）↔ T12（前端 readCookie）一致 ✓

**结论**：spec 全覆盖、无占位、类型一致。可交付执行。

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-07-26-web-auth-p0.md`. Two execution options:**

**1. Subagent-Driven（推荐）** — 每个 task 派一个全新 subagent 实现，task 间两阶段 review，迭代快、上下文干净。适合本计划（18 个 task 多数相互独立）。

**2. Inline Execution** — 在当前 session 用 executing-plans 批量执行 + checkpoint review。

**Which approach?**
