[project]
name = "supernova"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "supernova-whitebox",
    "supernova-blackbox",
    "supernova-combined",
    "supernova-multi",
    "supernova-web",
]

[tool.uv.sources]
supernova-core = { workspace = true }
supernova-whitebox = { workspace = true }
supernova-blackbox = { workspace = true }
supernova-combined = { workspace = true }
supernova-multi = { workspace = true }
supernova-web = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[tool.pytest.ini_options]
testpaths = ["packages/*/tests"]
asyncio_mode = "auto"
# Use importlib mode to avoid module name conflicts between packages with
# identically-named test files (e.g. whitebox/test_cli.py vs blackbox/test_cli.py)
addopts = "--import-mode=importlib"

[tool.ruff]
line-length = 120
target-version = "py312"

[dependency-groups]
dev = [
    "boto3>=1.43.17",
    "botocore>=1.43.17",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.15.1",
]
